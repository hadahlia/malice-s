extends Node

var level_id : int = 0

const FLASH_DUR = 0.15

#var score = 0

var score_multiplier = 1

var temp_score = 0:
	set(tmp_score):
		temp_score = tmp_score

#static func queue_free_children(node: Node) -> void:
	#for idx in node.get_child_count():
		#node.queue_free()
